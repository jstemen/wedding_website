<p id="notice"><%= notice %></p>



Please select which guests will be attending which events.

Events:
<%= semantic_form_for @invitation_group do |f| %>

    <%= f.semantic_fields_for :invitations do |invitation| %>
        <%= "#{invitation.object.event.name} - #{invitation.object.event.time}" %>
        <%= invitation.input :guests, :as => :check_boxes, :collection => @invitation_group.guests, :label => 'Guests coming to event' %>
    <% end %>

    <%= f.actions %>
<% end %>

