<%= form_for(@invitation_group) do |f| %>
  <% if @invitation_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invitation_group.errors.count, "error") %> prohibited this invitation_group from being saved:</h2>

      <ul>
      <% @invitation_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
